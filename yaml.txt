YAML имеет ключи и значения, но обрабатывает больше типов данных, чем JSON, включаа дату и время. Тут ситуация с функциями
наоборот, команда load() - преобразует строку YAML к данным Питрна, dump() - наоборот.